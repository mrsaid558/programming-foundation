"""
variables is an important defination and this consept is exist in all programming languages but there is a difference
between the way of writing

# variables in python
    
    -  variable names should contain only letters, numbers, and underscores
    
    - variables in python shouldn't start with numbers like => 3Name (x)
    
    - spaces are not allowed in the name, like => the name (x)
    
    - that names are case sensitive => Ahmed is not the same as ahmed
    
    - that names are case sensitive => if or else or elif (x) you can't make the name of your variable is simillar with the name of the keyword
"""
import keyword

name = "Ahmed"
print(name)

Name = "Khaled"
print(Name)

print(keyword.kwlist)